export const COLOR = {
    black: "#000",
    lightBlack: "#333",
    white: "#fff",
    darkWhite: "#ddd",
    point: "#ff0",
    pointHover: "#ff3",
    pointSelected: "#aaa",
    segmentSelected: "#888",
} as const;

export const DIMENSION = {
    defaultPointRadius: 4,
    defaultPointStrokeWidth: 1,
    defaultSegmentWidth: 2.5,
} as const;